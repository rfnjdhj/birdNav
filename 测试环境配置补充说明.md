# 测试环境配置补充说明

## 新增的依赖

在 `package.json` 中新增了以下开发依赖：

### 测试框架和测试库
- **vitest@^0.34.6
- **@vitest/ui@^0.34.6
- **jsdom@^22.1.0
- **@testing-library/react@^12.1.5
- **@testing-library/jest-dom@^5.16.5

## 配置文件

### 已创建的配置文件：
- **vitest.config.ts** - Vitest 测试配置文件
  - 配置了 jsdom 环境
  - 配置了路径别名 `@` 指向 `src`
  - 配置了测试设置文件

## 测试用例

### 工具函数测试：
- **src/utils/dateUtils.ts** - 日期格式化工具函数
- **src/utils/dateUtils.test.ts** - 日期格式化测试（5个测试用例）

### 组件测试：
- **src/component/Dock/CurrentTime/index.test.tsx** - CurrentTime 组件测试

## 运行测试的命令

```bash
# 运行测试（监听模式
npm run test

# 运行测试（单次运行）
npm run test:run

# 运行测试并生成覆盖率报告
npm run test:coverage

# 运行测试 UI 界面
npm run test:ui
```

## 验证步骤

1. **安装依赖：
```bash
npm install
```

2. **运行测试：
```bash
npm run test:run
```

3. **查看测试 UI：
```bash
npm run test:ui
```
