# 工程化配置优化说明

## 已修改的文件

### 1. 新增文件
- `.eslintrc.js` - ESLint 配置文件
- `.env.production` - 生产环境变量配置
- `src/constants/index.ts` - 环境变量常量文件
- `src/utils/dateUtils.ts` - 日期格式化工具函数
- `src/utils/dateUtils.test.ts` - 日期格式化工具函数测试文件
- `vitest.config.ts` - Vitest 测试配置文件
- `src/test/setup.ts` - 测试环境设置文件

### 2. 修改文件
- `package.json` - 添加了 ESLint、Prettier、测试脚本和 lint-staged 配置
- `vite.config.ts` - 启用了代码分割配置
- `.env.development` - 添加了开发环境 API 地址
- `src/main.tsx` - 修改了相对路径引用为使用 `@` 别名
- `src/App.tsx` - 修改了相对路径引用为使用 `@` 别名

## 如何验证配置生效

### 1. 代码规范检查
```bash
# 运行 ESLint 检查
npm run lint

# 运行 ESLint 修复
npm run lint:fix

# 运行 Prettier 格式化
npm run format

# 检查 Prettier 格式化
npm run format:check
```

### 2. 代码分割验证
构建项目后，检查 `dist/assets` 目录下的文件：
- `vendor.[hash].js` - React 相关依赖
- `utils.[hash].js` - 工具库
- `icons.[hash].js` - 图标库
- `index.[hash].js` - 应用主文件

### 3. 环境变量验证
- 开发环境：运行 `npm run dev`，查看控制台输出的 API 地址为 `http://localhost:3000
- 生产环境：运行 `npm run build`，查看构建后的 API 地址为 `https://api.birdnav.com

### 4. 测试运行
```bash
# 运行测试
npm run test

# 运行测试并生成覆盖率
npm run test:run

# 运行测试并查看覆盖率
npm run test:coverage

# 运行测试 UI
npm run test:ui
```

### 5. 路径别名验证
检查 `src/main.tsx` 和 `src/App.tsx` 文件中的导入语句，确认使用了 `@` 别名。

## 运行测试的命令

```bash
# 安装依赖
npm install

# 运行开发服务器
npm run dev

# 构建项目
npm run build

# 运行测试
npm run test

# 运行代码规范检查
npm run lint
```
